describe('sortableTable', function() {
  var result;

  beforeEach(function() {
    // Something do to before each test
  });

  it('should pass a basic test', function() {
    expect(1).to.equal(1);
  });

  it('should pass a basic test', function() {
    expect(1).to.equal(1);
  });

});
